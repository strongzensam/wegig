<h3><span class="text-blue"><%= link_to activity.owner.full_name, "/users/#{activity.owner.id}" %></span></h3><%=image_tag activity.owner.images.first.url %><h4> Created <%= link_to WorkExperience.find(activity.trackable_id).title, "/work_experiences/#{activity.trackable_id}" %></h4><br>
<p><%= WorkExperience.find(activity.trackable_id).description %></p>