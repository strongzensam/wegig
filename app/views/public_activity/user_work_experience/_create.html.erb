<h3><span class="text-blue"><%= link_to activity.owner.full_name, "/users/#{activity.owner.id}" %></span></h3><%=image_tag activity.owner.images.first.url %><h4><%= User.find("#{UserWorkExperience.find(activity.trackable_id).user_id}").full_name %> <%= UserWorkExperience.find(activity.trackable_id).role %> for <%= link_to WorkExperience.find("#{UserWorkExperience.find(activity.trackable_id).work_experience_id}").title, "/work_experiences/#{UserWorkExperience.find(activity.trackable_id).work_experience_id}" %></h4><br>
<p><%= UserWorkExperience.find(activity.trackable_id).description %></p>
<span class="right">created at <%=activity.created_at.strftime("%A, %d %b %Y %l:%M %p")%></span>